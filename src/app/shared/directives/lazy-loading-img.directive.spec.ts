import { LazyLoadingImgDirective } from './lazy-loading-img.directive';

describe('LazyLoadingImgDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadingImgDirective();
    expect(directive).toBeTruthy();
  });
});
