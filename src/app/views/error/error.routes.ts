import { Routes } from '@angular/router';
import { NotFoundComponent } from './not-found/not-found.component';

export const error_routes: Routes = [{ path: 'not-found', component: NotFoundComponent }];
